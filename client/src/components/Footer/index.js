import React from "react";

function Footer() {
  return (
    <div
      style={{
        height: "50px",
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
        fontSize: "1rem",
      }}
    >
      <p> Footer </p>
    </div>
  );
}

export default Footer;
